name: Python Tests

on:
  push:
    paths:
      - 'microservices/**'

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - run: pip install -r microservices/voice/requirements.txt
      - run: pytest microservices/voice/tests/